# @assistant-mk1/workbench-client

Platform-neutral, initially unpublished client for Assistant-mk1 product APIs.
It accepts an injected access-token provider and fetch implementation, and it
never owns credentials, UI state, or service secrets.
